# Required packages for Gesture Media Controller
opencv-python>=4.7.0
mediapipe>=0.10.0
pyautogui>=0.9.54
numpy>=1.24.0          # Needed by OpenCV and Mediapipe internally
pillow>=10.0.0         # Needed by PyAutoGUI for image handling

# To install all dependencies, run:
pip install -r requirements.txt
